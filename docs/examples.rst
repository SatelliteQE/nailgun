Examples
========

Here is an example of code that can be used to query the Satellite 6 API. It
does not make use of NailGun, and instead relies entirely on `Requests`_ and
standard library modules.

.. literalinclude:: get_org_envs.py

.. _Requests: http://docs.python-requests.org/en/latest/
